# Installation - {{projectName}}

Chemin local: `{{projectPath}}`

## Prerequis
- Node.js si le projet contient `package.json`.
- Variables documentees dans `.env.example` uniquement.

## Commandes
```powershell
npm install
npm run dev
```
